import {

} from '../constants/App'

const initialState = {
  theme: 'black'
}

export default function appState (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
